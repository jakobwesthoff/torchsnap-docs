---
title: Archive.torchsnap
description: Structure and contents of the .torchsnap gadget archive.
sidebar:
  order: 3
---

TODO: .torchsnap archive format documentation.

- What it is: ZIP archive with specific structure
- Required contents: `manifest.toml`, WASM binary
- Optional contents: SQL migrations, assets, frontend bundles (JS/CSS), icons
- How to build one (`just build-gadget <name>`)
- Installation locations:
  - System: `<resource_dir>/gadgets/` (bundled, not uninstallable)
  - User: `<app_data_dir>/gadgets/` (user-installed)
  - Dev: `<CARGO_MANIFEST_DIR>/../gadgets/` (debug builds only)
