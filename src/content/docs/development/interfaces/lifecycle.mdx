---
title: "lifecycle"
description: Gadget lifecycle management — enable, disable, and settings reactivity.
sidebar:
  order: 1
  badge:
    text: Export
    variant: note
---

TODO: Lifecycle export interface documentation.

- `enable() -> result<_, string>` — called on gadget activation, after SQL migrations
- `disable()` — called on deactivation, store drops after
- `on-setting-changed(key, value)` — reactive setting updates
