---
title: "filesystem"
description: Read-only filesystem access with glob patterns.
sidebar:
  order: 16
  badge:
    text: Import
    variant: tip
---

TODO: Filesystem import interface documentation.

- Permission: `[permissions.filesystem] read = [...]`
- Methods: `read-file`, `file-exists`, `metadata`
- Glob patterns (`*` single segment, `**` cross-segment)
- Path variable expansion (`${home}`, `${xdg-config}`, etc.)
