---
title: "path-resolver"
description: Resolve template variables at runtime.
sidebar:
  order: 20
  badge:
    text: Import
    variant: tip
---

TODO: Path resolver import interface documentation.

- Permission: `permissions.path-resolver = true`
- Variables: `${home}`, `${xdg-config}`, `${xdg-data}`, `${gadget-data}`, `${gadget-archive}`
