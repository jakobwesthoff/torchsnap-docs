---
// Intentionally empty. Starlight renders either its Hero component or a
// PageTitle banner — there is no way to suppress both without providing a
// hero override. The actual hero markup lives in src/pages/index.astro;
// this no-op override just prevents Starlight from rendering its own.
---
