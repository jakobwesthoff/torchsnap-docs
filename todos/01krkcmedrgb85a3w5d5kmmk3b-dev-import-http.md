# Import Interface: http

Path: `development/gadget-sdk/interfaces/http/`

Host → Gadget import. Synchronous (blocking) HTTP fetch.
- Permission: `[permissions.http] origins = [...]` or `["*"]`
- Origin validation and normalization at parse time
