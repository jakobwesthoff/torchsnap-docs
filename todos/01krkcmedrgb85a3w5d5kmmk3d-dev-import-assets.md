# Import Interface: assets

Path: `development/gadget-sdk/interfaces/assets/`

Host → Gadget import. Read files from inside the gadget's own `.torchsnap` archive.
- No permission required (always available, spatially scoped to archive)
