# Import Interface: path-resolver

Path: `development/gadget-sdk/interfaces/path-resolver/`

Host → Gadget import. Resolve `${...}` template variables at runtime.
- Permission: `permissions.path-resolver = true`
- Variables: `${home}`, `${xdg-config}`, `${xdg-data}`, `${gadget-data}`, `${gadget-archive}`
